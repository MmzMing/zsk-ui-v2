# 页头页脚

## 一、页头

### 1.1 前台页头（BasicLayout）

前台页头位于 `BasicLayout` 组件中，高度为 60px，固定在顶部，具有以下功能：

#### 1.1.1 左侧区域

| 功能 | 说明 |
|------|------|
| 品牌Logo | 显示网站Logo，支持hover动画，暗色模式下自动反转颜色 |
| 动态标题 | 使用 WordRotate 组件循环显示"知识库小破站"和"欢迎您的到来" |

#### 1.1.2 中间导航区域

| 功能 | 说明 |
|------|------|
| 桌面端导航 | 显示首页、知识库、制作简历、AI、关于博主等导航项，支持图标+文字展示 |
| 移动端导航 | 只显示图标，响应式适配 |
| 导航高亮 | 当前页面导航项显示主题色并带有底部边框 |

#### 1.1.3 右侧操作区

| 功能 | 说明 |
|------|------|
| 主题切换 | 点击切换浅色/深色主题，支持圆形遮罩动画 |
| 语言切换 | 切换中文/English双语 |
| 主题色选择 | 下拉菜单选择5种预设主题色（默认蓝、清新绿、神秘紫、活力橙、少女粉），支持自定义颜色 |
| 用户菜单（已登录） | 头像下拉菜单，包含后台管理、个人中心、退出登录 |
| 登录按钮（未登录） | 跳转到登录页面 |

#### 1.1.4 交互特性

| 特性 | 说明 |
|------|------|
| 滚动隐藏 | 向下滚动时页头自动隐藏，向上滚动时显示 |
| 粘性定位 | 始终固定在顶部，z-index为30 |
| 平滑过渡 | 所有状态变化都有0.2秒的过渡动画 |

---

### 1.2 后台页头（AdminLayout）

后台页头位于 `AdminLayout` 组件中，高度为 56px，左右布局，具有以下功能：

#### 1.2.1 左右布局（侧边导航布局）

| 功能 | 说明 |
|------|------|
| 移动端菜单 | 点击弹出侧边栏菜单（仅移动端显示） |
| 面包屑导航 | 显示当前页面层级路径（如：后台管理 > 数据概览 > 仪表盘） |

#### 1.2.2 右侧为内容区的经典后台布局，分三层结构

##### 顶部操作栏

| 功能 | 说明 |
|------|------|
| 全屏切换 | 切换全屏/退出全屏模式 |
| 消息中心 | 消息通知入口（开发中） |
| 系统设置 | 打开系统设置面板 |
| 用户菜单 | 头像下拉菜单，包含返回前台、个人中心、退出登录 |

##### 顶部导航栏

| 功能                      | 说明                                      |
| ------------------------- | ----------------------------------------- |
| 顶部导航（水平/混合布局） | 显示管理模块分类，hover显示子菜单下拉列表 |

##### 下方内容区

| 功能     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 主内容区 | 根据左侧菜单栏位进行切换后更改的内容区域，主要ui设计为卡片式（Card-based）布局 |

---

## 二、页脚

### 2.1 前台页脚（BasicLayout）

前台页脚仅在前台页面显示，具有以下功能：

#### 2.1.1 左侧信息区

| 功能 | 说明 |
|------|------|
| 版权声明 | 显示"© 2026 知识库小破站" |
| 站点简介 | "一个专注知识整理和成长记录的小站" |
| 运行天数 | 显示站点已运行天数，从2026-01-01开始计算 |
| 访问统计 | 累计访问人数、累计访问次数（开发中） |
| 免责声明 | 说明本站内容用途和引用规范 |

#### 2.1.2 右侧链接区（三列布局）

**支持列**

| 链接 | 说明 |
|------|------|
| 问题解答 | 常见问题解答（开发中） |
| 联系方式 | 跳转到关于博主页面 |
| 反馈 | 用户反馈入口（开发中） |
| 更新日志 | 跳转到GitHub仓库 |

**社交列**

| 链接 | 说明 |
|------|------|
| GitHub | 跳转到GitHub仓库 |
| QQ | 打开QQ群聊 |
| Twitter/X | 跳转到Twitter |
| Discord | 跳转到Discord |

**更多列**

| 链接 | 说明 |
|------|------|
| 隐私政策 | 打开隐私政策弹窗 |
| 用户协议 | 打开用户协议弹窗 |
| 扩展 | 浏览器扩展（开发中） |

---

## 三、其他组件

### 3.1 回到顶部按钮

| 功能 | 说明 |
|------|------|
| 显示条件 | 页面滚动超过400px时显示 |
| 位置 | 右下角固定，距离底部80px、右侧24px |
| 动画 | 淡入淡出、缩放动画 |
| 功能 | 点击平滑滚动到页面顶部 |

### 3.2 多标签页栏

| 功能 | 说明 |
|------|------|
| 显示条件 | 启用"多标签页"设置时显示 |
| 位置 | 页头下方，高度36px |
| 功能 | 显示已访问的页面标签，支持点击切换、关闭标签 |
| 右键菜单 | 支持关闭当前、关闭其他、关闭全部 |

### 3.3 面包屑导航

| 功能 | 说明 |
|------|------|
| 显示条件 | 启用"面包屑导航"设置时显示 |
| 位置 | 多标签页栏下方，高度32px |
| 内容 | 显示首页 > 当前页面路径 |

---

## 五、技术要点总结

1. **响应式设计**：完美适配桌面端和移动端
2. **动画效果**：使用 Framer Motion 实现流畅的过渡动画
3. **状态管理**：使用 Zustand 管理布局、主题等全局状态
4. **本地持久化**：所有用户配置自动保存到 localStorage
5. **主题适配**：所有组件支持浅色/深色双主题
6. **灵活配置**：多种布局模式、配色方案可选
